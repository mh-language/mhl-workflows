{"features":[{"dependsOn":[],"description":"Provide the durable local Postgres foundation and safe database configuration.. Observable outcome: A fresh or retained Compose database becomes healthy, the API can connect using the configured connection, and startup creates the valid tasks schema without deleting retained data.. Happy path: Compose reports healthy Postgres, the API opens its configured data source, applies CREATE TABLE IF NOT EXISTS, and serves against the retained database.. Failure path: Unavailable or misconfigured Postgres fails through the sanitized dependency boundary; credentials and raw driver details are absent from responses and logs..","id":1,"implementationContext":{"acceptance":["Starting against both a fresh database and a retained volume succeeds deterministically, preserves existing tasks across an API-only restart, and never emits connection credentials or raw SQL details.","AC-7: Given A fresh Postgres database or an existing database with tasks is available. When The API starts, initializes the schema, creates tasks, and is restarted without removing the database volume. Then Schema initialization succeeds idempotently, records satisfy the identifier/title/status rules, and previously created tasks remain queryable after restart","AC-9: Given TODO_DB_CONNECTION is set or the documented local default is used. When The API and tests connect to Postgres and execute database operations. Then The configured connection is used, all database values are parameterized, and credentials do not appear in source-controlled output, client responses, or logs","AC-12: Given The verification flow is run once against a fresh database and again against a retained Compose volume. When Both runs perform readiness, schema initialization, and tests. Then Both runs complete deterministically, schema setup is idempotent, and retained data is not implicitly deleted"],"constraints":["out of scope: Endpoint-specific HTTP behavior","out of scope: Authentication, cloud deployment, backups, and schema migrations","tasks table stores positive bigint id, trimmed non-empty title, and status in pendente or concluida","database connection comes from TODO_DB_CONNECTION or the documented local-only default","schema bootstrap is idempotent and API-only restarts retain the Compose volume"],"decisions":["ADR-3: Direct Npgsql per slice. Decision: Use one singleton NpgsqlDataSource and explicit parameterized commands owned by each slice.. Rationale: Keeps SQL and mapping visible and avoids a generic persistence abstraction while using real Postgres.","ADR-4: Postgres in Docker Compose. Decision: Run one pinned Postgres service with healthcheck and persistent volume as the local datastore.. Rationale: Provides real persistence and integration fidelity with reproducible local setup.","ADR-5: API-owned idempotent schema bootstrap. Decision: Apply the initial tasks table definition at API startup with an idempotent bootstrap.. Rationale: Supports fresh and retained databases without manual setup; later evolution remains an explicit decision.","ADR-6: Minimal shared kernel. Decision: Share only task/status domain types, data-source creation, configuration, and common errors.. Rationale: Prevents endpoint-specific rules and queries from becoming shared coupling.","ADR-9: Environment-based configuration. Decision: Read TODO_DB_CONNECTION and provide only a documented local default.. Rationale: Enables repeatable local execution without hardcoded credentials or source-controlled secrets."],"files":["app/docker-compose.yml, app/src/TodoApp.Api/Infrastructure/Database/, app/src/TodoApp.Api/Program.cs"],"requirements":["RF-7: The application shall store each task in Postgres with an identifier, normalized non-empty title, and pending/completed status, enforce the valid status representation, initialize the schema idempotently, and preserve task data across an API-only restart.","RF-9: The application shall read its database connection from TODO_DB_CONNECTION, provide only a documented local default, parameterize every database operation, and avoid placing credentials in source-controlled responses or logs.","RQ-3: Local execution shall be repeatable and idempotent: readiness checks, schema initialization, and test setup shall tolerate a fresh database and a retained Compose volume without implicitly deleting retained data."]},"priority":1,"references":["RF-7","RF-9","RQ-3","ADR-3","ADR-4","ADR-5","ADR-6","ADR-9"],"status":"pending","title":"Provide the durable local Postgres foundation and safe database configuration."},{"dependsOn":[1],"description":"Create pending tasks through the canonical HTTP contract.. Observable outcome: A valid create request returns exactly the canonical task representation with normalized title and pending status, and the same row exists in Postgres.. Happy path: The slice trims a valid title, inserts a pending row, maps only the public fields, and returns 201 with Location.. Failure path: Missing, empty-after-trim, or otherwise invalid titles are rejected before mutation with sanitized Problem Details..","id":2,"implementationContext":{"acceptance":["A real HTTP integration test verifies status, JSON shape, Location, normalization, and the corresponding real Postgres row; pure tests cover title rules.","AC-1: Given The API and healthy Postgres are running and the client submits a valid task title. When The client calls the create-task endpoint. Then The response has the documented success status and canonical JSON task shape, with a unique identifier, normalized title, and pending status; the same record is present in Postgres","AC-10: Given The implementation is reviewed endpoint by endpoint. When A reviewer traces an endpoint from HTTP input through validation, persistence, response, and tests. Then The behavior is contained in one vertical slice, with only justified shared cross-cutting code and no generic Controller, Service, or Repository layer","AC-11: Given The test suite is run through the documented verification flow. When Unit and integration tests execute. Then Pure rule tests run without external dependencies and lifecycle/failure integration tests use real HTTP and real Postgres with no mocks or in-memory persistence"],"constraints":["out of scope: Listing, completion, editing, and removal","out of scope: Generic service or repository layers","POST /tasks accepts JSON titulo","success is 201 with id, titulo, and status=pendente","all client-supplied database values are parameterized"],"decisions":["ADR-1: Vertical slices per endpoint. Decision: Keep HTTP handling, validation, SQL, mapping, and tests in the slice that owns each operation.. Rationale: Preserves cohesion and endpoint traceability while avoiding generic layers and their coupling.","ADR-2: .NET/C# and ASP.NET Core. Decision: Use the mandatory .NET/C# ASP.NET Core stack and pin a supported LTS SDK.. Rationale: Provides one consistent implementation and test ecosystem within the approved boundary.","ADR-3: Direct Npgsql per slice. Decision: Use one singleton NpgsqlDataSource and explicit parameterized commands owned by each slice.. Rationale: Keeps SQL and mapping visible and avoids a generic persistence abstraction while using real Postgres.","ADR-7: Two-level xUnit verification. Decision: Use pure unit tests plus real HTTP/Postgres integration tests.. Rationale: Combines fast rule diagnosis with contract and persistence confidence without mocks or in-memory storage.","ADR-10: Canonical JSON and Problem Details. Decision: Stabilize id, titulo, status, status values, HTTP outcomes, and sanitized application/problem+json errors.. Rationale: Improves interoperability and makes failure behavior testable without exposing internal details."],"files":["app/src/TodoApp.Api/Features/AddTask/, app/tests/TodoApp.UnitTests/Features/AddTask/, app/tests/TodoApp.IntegrationTests/"],"requirements":["RF-1: The API shall create a task from a valid title, assign a unique identifier, normalize and persist the title with pending status, and return the canonical task representation with the documented success status and JSON shape.","RQ-1: Each endpoint shall be implemented as an independently traceable vertical slice, with only a minimal shared kernel for proven cross-cutting concerns and no generic Controller, Service, or Repository layers.","RQ-2: Automated verification shall include pure unit tests for normalization, validation, and state rules plus integration tests using real HTTP and real Postgres, with no mocks or in-memory persistence substitutes."]},"priority":2,"references":["RF-1","RQ-1","RQ-2","ADR-1","ADR-2","ADR-3","ADR-7","ADR-10"],"status":"pending","title":"Create pending tasks through the canonical HTTP contract."},{"dependsOn":[1,2],"description":"List tasks in identifier order with an exact pending/completed filter.. Observable outcome: The list endpoint returns all tasks or exactly those matching the documented status filter, in ascending identifier order.. Happy path: The slice validates the optional filter, executes a parameterized ordered query, and maps each row to id, titulo, and status.. Failure path: An unsupported status value returns sanitized 400 Problem Details and leaves the database unchanged..","id":3,"implementationContext":{"acceptance":["Real HTTP/Postgres tests prove unfiltered listing, both exact filters, ordering, empty results, and invalid-filter behavior.","AC-2: Given Postgres contains pending and completed tasks. When The client calls the list endpoint with no filter and with each documented status filter. Then Each response contains exactly the matching canonical tasks and excludes tasks with other statuses","AC-10: Given The implementation is reviewed endpoint by endpoint. When A reviewer traces an endpoint from HTTP input through validation, persistence, response, and tests. Then The behavior is contained in one vertical slice, with only justified shared cross-cutting code and no generic Controller, Service, or Repository layer","AC-11: Given The test suite is run through the documented verification flow. When Unit and integration tests execute. Then Pure rule tests run without external dependencies and lifecycle/failure integration tests use real HTTP and real Postgres with no mocks or in-memory persistence"],"constraints":["out of scope: Pagination, sorting options, and task mutation","out of scope: Additional task fields","GET /tasks returns 200 and a canonical task array","status accepts only pendente or concluida","invalid filters return 400 Problem Details without querying or mutating data"],"decisions":["ADR-1: Vertical slices per endpoint. Decision: Keep HTTP handling, validation, SQL, mapping, and tests in the slice that owns each operation.. Rationale: Preserves cohesion and endpoint traceability while avoiding generic layers and their coupling.","ADR-2: .NET/C# and ASP.NET Core. Decision: Use the mandatory .NET/C# ASP.NET Core stack and pin a supported LTS SDK.. Rationale: Provides one consistent implementation and test ecosystem within the approved boundary.","ADR-3: Direct Npgsql per slice. Decision: Use one singleton NpgsqlDataSource and explicit parameterized commands owned by each slice.. Rationale: Keeps SQL and mapping visible and avoids a generic persistence abstraction while using real Postgres.","ADR-7: Two-level xUnit verification. Decision: Use pure unit tests plus real HTTP/Postgres integration tests.. Rationale: Combines fast rule diagnosis with contract and persistence confidence without mocks or in-memory storage.","ADR-10: Canonical JSON and Problem Details. Decision: Stabilize id, titulo, status, status values, HTTP outcomes, and sanitized application/problem+json errors.. Rationale: Improves interoperability and makes failure behavior testable without exposing internal details."],"files":["app/src/TodoApp.Api/Features/ListTasks/, app/tests/TodoApp.UnitTests/Features/ListTasks/, app/tests/TodoApp.IntegrationTests/"],"requirements":["RF-2: The API shall list persisted tasks and support the documented pending/completed status filter, returning canonical task representations from Postgres in the documented JSON shape.","RQ-1: Each endpoint shall be implemented as an independently traceable vertical slice, with only a minimal shared kernel for proven cross-cutting concerns and no generic Controller, Service, or Repository layers.","RQ-2: Automated verification shall include pure unit tests for normalization, validation, and state rules plus integration tests using real HTTP and real Postgres, with no mocks or in-memory persistence substitutes."]},"priority":3,"references":["RF-2","RQ-1","RQ-2","ADR-1","ADR-2","ADR-3","ADR-7","ADR-10"],"status":"pending","title":"List tasks in identifier order with an exact pending/completed filter."},{"dependsOn":[1,2],"description":"Complete tasks through an idempotent canonical state transition.. Observable outcome: A pending task becomes concluida in the response and Postgres, while repeating completion returns the documented idempotent success.. Happy path: The slice validates the id, updates status to concluida, maps the task, and returns 200; a second call returns the same completed representation.. Failure path: Invalid identifiers are rejected before SQL and a missing task produces sanitized 404 without unintended mutation..","id":4,"implementationContext":{"acceptance":["Unit and real HTTP/Postgres tests verify pending-to-completed persistence, repeated completion, invalid ids, missing tasks, and sanitized errors.","AC-3: Given A pending task exists. When The client calls the complete endpoint for its identifier. Then The response returns the task as completed, and a subsequent list or retrieval observes completed status persisted in Postgres; the documented repeated-completion behavior is returned on a second call","AC-10: Given The implementation is reviewed endpoint by endpoint. When A reviewer traces an endpoint from HTTP input through validation, persistence, response, and tests. Then The behavior is contained in one vertical slice, with only justified shared cross-cutting code and no generic Controller, Service, or Repository layer","AC-11: Given The test suite is run through the documented verification flow. When Unit and integration tests execute. Then Pure rule tests run without external dependencies and lifecycle/failure integration tests use real HTTP and real Postgres with no mocks or in-memory persistence"],"constraints":["out of scope: Concurrency guarantees beyond the local scope","out of scope: Editing or removing tasks","PATCH /tasks/{id}/complete returns 200 with the completed canonical task","invalid ids return 400 and missing ids return 404 Problem Details","completion is idempotent for an already completed task"],"decisions":["ADR-1: Vertical slices per endpoint. Decision: Keep HTTP handling, validation, SQL, mapping, and tests in the slice that owns each operation.. Rationale: Preserves cohesion and endpoint traceability while avoiding generic layers and their coupling.","ADR-3: Direct Npgsql per slice. Decision: Use one singleton NpgsqlDataSource and explicit parameterized commands owned by each slice.. Rationale: Keeps SQL and mapping visible and avoids a generic persistence abstraction while using real Postgres.","ADR-7: Two-level xUnit verification. Decision: Use pure unit tests plus real HTTP/Postgres integration tests.. Rationale: Combines fast rule diagnosis with contract and persistence confidence without mocks or in-memory storage.","ADR-10: Canonical JSON and Problem Details. Decision: Stabilize id, titulo, status, status values, HTTP outcomes, and sanitized application/problem+json errors.. Rationale: Improves interoperability and makes failure behavior testable without exposing internal details."],"files":["app/src/TodoApp.Api/Features/CompleteTask/, app/tests/TodoApp.UnitTests/Features/CompleteTask/, app/tests/TodoApp.IntegrationTests/"],"requirements":["RF-3: The API shall complete an existing pending task through its canonical endpoint, persist the completed status, and return the updated canonical task; repeated completion shall follow the documented conflict or idempotency behavior.","RQ-1: Each endpoint shall be implemented as an independently traceable vertical slice, with only a minimal shared kernel for proven cross-cutting concerns and no generic Controller, Service, or Repository layers.","RQ-2: Automated verification shall include pure unit tests for normalization, validation, and state rules plus integration tests using real HTTP and real Postgres, with no mocks or in-memory persistence substitutes."]},"priority":4,"references":["RF-3","RQ-1","RQ-2","ADR-1","ADR-3","ADR-7","ADR-10"],"status":"pending","title":"Complete tasks through an idempotent canonical state transition."},{"dependsOn":[1,2],"description":"Edit an existing task title while preserving its identifier and status.. Observable outcome: A valid edit returns and persists the normalized replacement title without changing the task id or status.. Happy path: The slice validates and trims titulo, updates only title, and returns the existing id and status with the normalized replacement.. Failure path: Invalid input is rejected before mutation and a nonexistent task produces sanitized 404..","id":5,"implementationContext":{"acceptance":["Real HTTP/Postgres tests edit both pending and completed tasks and verify title normalization, status preservation, row state, and failure behavior.","AC-4: Given A task exists and the client submits a valid replacement title. When The client calls the edit endpoint for its identifier. Then The response and Postgres record contain the normalized replacement title while preserving the task identifier and valid status","AC-10: Given The implementation is reviewed endpoint by endpoint. When A reviewer traces an endpoint from HTTP input through validation, persistence, response, and tests. Then The behavior is contained in one vertical slice, with only justified shared cross-cutting code and no generic Controller, Service, or Repository layer","AC-11: Given The test suite is run through the documented verification flow. When Unit and integration tests execute. Then Pure rule tests run without external dependencies and lifecycle/failure integration tests use real HTTP and real Postgres with no mocks or in-memory persistence"],"constraints":["out of scope: Status changes, history, versioning, and concurrency guarantees","out of scope: Additional editable fields","PUT /tasks/{id} accepts JSON titulo and returns 200 with the canonical task","valid status is preserved","invalid id/title returns 400 and missing task returns 404 Problem Details"],"decisions":["ADR-1: Vertical slices per endpoint. Decision: Keep HTTP handling, validation, SQL, mapping, and tests in the slice that owns each operation.. Rationale: Preserves cohesion and endpoint traceability while avoiding generic layers and their coupling.","ADR-3: Direct Npgsql per slice. Decision: Use one singleton NpgsqlDataSource and explicit parameterized commands owned by each slice.. Rationale: Keeps SQL and mapping visible and avoids a generic persistence abstraction while using real Postgres.","ADR-7: Two-level xUnit verification. Decision: Use pure unit tests plus real HTTP/Postgres integration tests.. Rationale: Combines fast rule diagnosis with contract and persistence confidence without mocks or in-memory storage.","ADR-10: Canonical JSON and Problem Details. Decision: Stabilize id, titulo, status, status values, HTTP outcomes, and sanitized application/problem+json errors.. Rationale: Improves interoperability and makes failure behavior testable without exposing internal details."],"files":["app/src/TodoApp.Api/Features/EditTask/, app/tests/TodoApp.UnitTests/Features/EditTask/, app/tests/TodoApp.IntegrationTests/"],"requirements":["RF-4: The API shall edit the title of an existing task only when the supplied identifier and title are valid, normalize and persist the new title, and return the updated canonical task.","RQ-1: Each endpoint shall be implemented as an independently traceable vertical slice, with only a minimal shared kernel for proven cross-cutting concerns and no generic Controller, Service, or Repository layers.","RQ-2: Automated verification shall include pure unit tests for normalization, validation, and state rules plus integration tests using real HTTP and real Postgres, with no mocks or in-memory persistence substitutes."]},"priority":5,"references":["RF-4","RQ-1","RQ-2","ADR-1","ADR-3","ADR-7","ADR-10"],"status":"pending","title":"Edit an existing task title while preserving its identifier and status."},{"dependsOn":[1,2,3],"description":"Remove an existing task through the canonical delete contract.. Observable outcome: Deleting an existing task returns 204 with no body and the task is absent from later results.. Happy path: The slice validates the id, deletes one row, checks the affected count, and returns 204.. Failure path: Invalid or nonexistent identifiers do not mutate data and return the documented sanitized Problem Details..","id":6,"implementationContext":{"acceptance":["A real HTTP/Postgres test proves 204 no-body behavior, physical absence after deletion, invalid-id rejection, and missing-task handling.","AC-5: Given A task exists. When The client calls the remove endpoint for its identifier and then lists or retrieves tasks. Then The remove response has the documented success status and the task is absent from subsequent results","AC-10: Given The implementation is reviewed endpoint by endpoint. When A reviewer traces an endpoint from HTTP input through validation, persistence, response, and tests. Then The behavior is contained in one vertical slice, with only justified shared cross-cutting code and no generic Controller, Service, or Repository layer","AC-11: Given The test suite is run through the documented verification flow. When Unit and integration tests execute. Then Pure rule tests run without external dependencies and lifecycle/failure integration tests use real HTTP and real Postgres with no mocks or in-memory persistence"],"constraints":["out of scope: Soft deletion, audit history, restore, and bulk operations","out of scope: Backup and retention workflows","DELETE /tasks/{id} returns 204 with no response body","invalid ids return 400 and missing ids return sanitized 404","successful deletion is visible in subsequent listing or retrieval"],"decisions":["ADR-1: Vertical slices per endpoint. Decision: Keep HTTP handling, validation, SQL, mapping, and tests in the slice that owns each operation.. Rationale: Preserves cohesion and endpoint traceability while avoiding generic layers and their coupling.","ADR-3: Direct Npgsql per slice. Decision: Use one singleton NpgsqlDataSource and explicit parameterized commands owned by each slice.. Rationale: Keeps SQL and mapping visible and avoids a generic persistence abstraction while using real Postgres.","ADR-7: Two-level xUnit verification. Decision: Use pure unit tests plus real HTTP/Postgres integration tests.. Rationale: Combines fast rule diagnosis with contract and persistence confidence without mocks or in-memory storage.","ADR-10: Canonical JSON and Problem Details. Decision: Stabilize id, titulo, status, status values, HTTP outcomes, and sanitized application/problem+json errors.. Rationale: Improves interoperability and makes failure behavior testable without exposing internal details."],"files":["app/src/TodoApp.Api/Features/RemoveTask/, app/tests/TodoApp.UnitTests/Features/RemoveTask/, app/tests/TodoApp.IntegrationTests/"],"requirements":["RF-5: The API shall remove an existing task through its canonical endpoint and return the documented success response; subsequent retrieval or listing shall no longer expose the removed task.","RQ-1: Each endpoint shall be implemented as an independently traceable vertical slice, with only a minimal shared kernel for proven cross-cutting concerns and no generic Controller, Service, or Repository layers.","RQ-2: Automated verification shall include pure unit tests for normalization, validation, and state rules plus integration tests using real HTTP and real Postgres, with no mocks or in-memory persistence substitutes."]},"priority":6,"references":["RF-5","RQ-1","RQ-2","ADR-1","ADR-3","ADR-7","ADR-10"],"status":"pending","title":"Remove an existing task through the canonical delete contract."},{"dependsOn":[1],"description":"Enforce the shared validation and sanitized failure boundary across all slices.. Observable outcome: Every documented failure returns stable sanitized Problem Details and no sensitive or internal details reach clients or logs.. Happy path: Endpoint slices return canonical success responses while the shared boundary consistently serializes expected failures.. Failure path: Induced validation, missing-task, dependency, and unexpected failures are mapped to stable sanitized responses and redacted diagnostics..","id":7,"implementationContext":{"acceptance":["Failure-focused unit and integration tests verify status/content type/stable fields, no unintended mutation, and absence of forbidden details in response and logs.","AC-6: Given The client submits an invalid identifier, invalid title or filter, a missing task identifier, or an invalid state transition. When The client calls the applicable endpoint. Then The API performs no unintended mutation and returns the documented status with sanitized Problem Details containing stable diagnostic fields and no stack trace or SQL/credential details","AC-13: Given Validation, not-found, conflict, database, and unexpected failures are induced in a local run. When The API returns errors and writes diagnostics. Then Clients receive only the documented sanitized Problem Details and neither clients nor logs contain credentials, SQL details, stack traces, or unrelated internal data"],"constraints":["out of scope: Authentication and authorization","out of scope: Production observability, external secret management, and public exposure","Errors use application/problem+json with type, title, status, detail, and instance","Validation occurs before mutable database operations","Public and logged diagnostics omit credentials, SQL, stack traces, raw driver details, and unrelated task content"],"decisions":["ADR-1: Vertical slices per endpoint. Decision: Keep HTTP handling, validation, SQL, mapping, and tests in the slice that owns each operation.. Rationale: Preserves cohesion and endpoint traceability while avoiding generic layers and their coupling.","ADR-3: Direct Npgsql per slice. Decision: Use one singleton NpgsqlDataSource and explicit parameterized commands owned by each slice.. Rationale: Keeps SQL and mapping visible and avoids a generic persistence abstraction while using real Postgres.","ADR-6: Minimal shared kernel. Decision: Share only task/status domain types, data-source creation, configuration, and common errors.. Rationale: Prevents endpoint-specific rules and queries from becoming shared coupling.","ADR-9: Environment-based configuration. Decision: Read TODO_DB_CONNECTION and provide only a documented local default.. Rationale: Enables repeatable local execution without hardcoded credentials or source-controlled secrets.","ADR-10: Canonical JSON and Problem Details. Decision: Stabilize id, titulo, status, status values, HTTP outcomes, and sanitized application/problem+json errors.. Rationale: Improves interoperability and makes failure behavior testable without exposing internal details."],"files":["app/src/TodoApp.Api/Infrastructure/Errors/, app/src/TodoApp.Api/Program.cs, app/tests/TodoApp.UnitTests/, app/tests/TodoApp.IntegrationTests/"],"requirements":["RF-6: The API shall validate identifiers, required fields, title constraints, status-filter values, not-found conditions, and applicable state conflicts before mutation, using the documented HTTP status codes and sanitized Problem Details failure contract.","RQ-4: Failure handling shall map expected and unexpected failures to stable sanitized Problem Details and shall not expose credentials, task content beyond the response contract, SQL details, stack traces, or other internal failure information to clients or logs."]},"priority":7,"references":["RF-6","RQ-4","ADR-1","ADR-3","ADR-6","ADR-9","ADR-10"],"status":"pending","title":"Enforce the shared validation and sanitized failure boundary across all slices."},{"dependsOn":[2,3,4,5,6,7],"description":"Provide one repeatable local command that verifies the complete application lifecycle.. Observable outcome: One documented command waits for healthy Postgres, starts the API with schema ready, runs both suites, propagates failures, and succeeds on fresh and retained-volume runs.. Happy path: The command brings up the local stack, runs isolated unit and integration coverage, repeats successfully, and verifies API-host recreation with retained data.. Failure path: Readiness or test failure stops the aggregate flow with a nonzero exit code and does not falsely report success from textual output..","id":8,"implementationContext":{"acceptance":["Fresh-database and retained-volume executions both complete deterministically, cover the complete lifecycle and failures, and demonstrate restart durability with real infrastructure.","AC-8: Given Docker Compose and the documented project prerequisites are available. When An operator runs the single documented verification command. Then The command waits for database readiness, starts the API, applies schema setup, executes all unit and real HTTP/Postgres integration tests, and reports success without manual ordering","AC-11: Given The test suite is run through the documented verification flow. When Unit and integration tests execute. Then Pure rule tests run without external dependencies and lifecycle/failure integration tests use real HTTP and real Postgres with no mocks or in-memory persistence","AC-12: Given The verification flow is run once against a fresh database and again against a retained Compose volume. When Both runs perform readiness, schema initialization, and tests. Then Both runs complete deterministically, schema setup is idempotent, and retained data is not implicitly deleted"],"constraints":["out of scope: CI/CD, production deployment, cloud operations, and SLA measurement","out of scope: Implicit deletion of retained volumes","The aggregate verification command waits for database health and returns nonzero on any failed suite","Unit tests have no external dependency; integration tests use real HTTP and real Postgres","Integration setup is serial and idempotent without deleting the retained Compose volume"],"decisions":["ADR-2: .NET/C# and ASP.NET Core. Decision: Use the mandatory .NET/C# ASP.NET Core stack and pin a supported LTS SDK.. Rationale: Provides one consistent implementation and test ecosystem within the approved boundary.","ADR-4: Postgres in Docker Compose. Decision: Run one pinned Postgres service with healthcheck and persistent volume as the local datastore.. Rationale: Provides real persistence and integration fidelity with reproducible local setup.","ADR-5: API-owned idempotent schema bootstrap. Decision: Apply the initial tasks table definition at API startup with an idempotent bootstrap.. Rationale: Supports fresh and retained databases without manual setup; later evolution remains an explicit decision.","ADR-7: Two-level xUnit verification. Decision: Use pure unit tests plus real HTTP/Postgres integration tests.. Rationale: Combines fast rule diagnosis with contract and persistence confidence without mocks or in-memory storage.","ADR-8: Serial integration isolation. Decision: Run integration tests in a non-parallel collection and truncate/reset before each case.. Rationale: Provides deterministic repeated runs for the small local scope."],"files":["app/init.sh, app/verify-feature.sh, app/tests/TodoApp.IntegrationTests/Fixtures/, app/README.md"],"requirements":["RF-8: The project shall provide one documented, repeatable Docker Compose startup and verification flow that waits for healthy pinned Postgres, initializes a fresh database automatically, starts the API, and runs unit and real HTTP/Postgres integration tests without manual sequencing.","RQ-2: Automated verification shall include pure unit tests for normalization, validation, and state rules plus integration tests using real HTTP and real Postgres, with no mocks or in-memory persistence substitutes.","RQ-3: Local execution shall be repeatable and idempotent: readiness checks, schema initialization, and test setup shall tolerate a fresh database and a retained Compose volume without implicitly deleting retained data."]},"priority":8,"references":["RF-8","RQ-2","RQ-3","ADR-2","ADR-4","ADR-5","ADR-7","ADR-8"],"status":"pending","title":"Provide one repeatable local command that verifies the complete application lifecycle."}]}