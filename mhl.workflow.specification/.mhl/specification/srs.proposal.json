{
  "schema": "iao/srs/v1",
  "prdDigest": "sha256:fdf310c9b45829b8211875e77b3414883d32340496989f2960ff5b8f8b82b7ec",
  "functionalRequirements": [
    {
      "id": "RF-1",
      "goalIds": ["G-1"],
      "statement": "The API shall accept an unauthenticated JSON POST request to create a task with a non-empty title, persist the task with a generated identifier and initial incomplete status, and return the created task as stable JSON.",
      "dependsOn": ["RF-6", "RF-9"],
      "acceptanceIds": ["AC-1", "AC-7"]
    },
    {
      "id": "RF-2",
      "goalIds": ["G-1"],
      "statement": "The API shall accept an unauthenticated JSON GET request to list tasks and return stable JSON containing each task's id, title, and status.",
      "dependsOn": ["RF-6", "RF-9"],
      "acceptanceIds": ["AC-2"]
    },
    {
      "id": "RF-3",
      "goalIds": ["G-1"],
      "statement": "The API shall support filtering the task list by task status and shall return only tasks matching the requested valid status value.",
      "dependsOn": ["RF-2"],
      "acceptanceIds": ["AC-3", "AC-7"]
    },
    {
      "id": "RF-4",
      "goalIds": ["G-1"],
      "statement": "The API shall accept an unauthenticated request to complete an existing task, persist its status as complete, and return the updated task as stable JSON.",
      "dependsOn": ["RF-6", "RF-9"],
      "acceptanceIds": ["AC-4", "AC-8"]
    },
    {
      "id": "RF-5",
      "goalIds": ["G-1"],
      "statement": "The API shall accept an unauthenticated JSON request to edit an existing task title, persist the updated title, and return the updated task as stable JSON.",
      "dependsOn": ["RF-6", "RF-9"],
      "acceptanceIds": ["AC-5", "AC-8"]
    },
    {
      "id": "RF-6",
      "goalIds": ["G-2"],
      "statement": "The API shall persist each task's id, title, and status in Postgres and shall read those records from Postgres for subsequent requests, preserving them when only the API process is stopped and restarted.",
      "dependsOn": ["RF-7", "RF-8"],
      "acceptanceIds": ["AC-6", "AC-9"]
    },
    {
      "id": "RF-7",
      "goalIds": ["G-3"],
      "statement": "On local startup, the application shall initialize the required task schema in Postgres automatically and shall be safe to run against an already initialized schema.",
      "dependsOn": ["RF-8"],
      "acceptanceIds": ["AC-10"]
    },
    {
      "id": "RF-8",
      "goalIds": ["G-3"],
      "statement": "The application shall obtain its Postgres connection from TODO_DB_CONNECTION, document a safe local default without committing credentials, and fail startup or requests with a sanitized diagnostic when the dependency is unavailable.",
      "dependsOn": [],
      "acceptanceIds": ["AC-11", "AC-12"]
    },
    {
      "id": "RF-9",
      "goalIds": ["G-4"],
      "statement": "Each task operation shall expose a stable JSON success contract and stable Problem Details responses for invalid input, a missing task, and dependency failures, without credentials, stack traces, or provider-internal details.",
      "dependsOn": [],
      "acceptanceIds": ["AC-7", "AC-8", "AC-12"]
    },
    {
      "id": "RF-10",
      "goalIds": ["G-1"],
      "statement": "The API shall accept an unauthenticated request to remove an existing task and shall return the documented successful deletion response; attempting to remove a missing task shall return the documented Problem Details response.",
      "dependsOn": ["RF-6", "RF-9"],
      "acceptanceIds": ["AC-13"]
    }
  ],
  "qualityRequirements": [
    {
      "id": "RQ-1",
      "goalIds": ["G-3"],
      "statement": "The local environment shall be reproducible through Docker Compose with one Postgres service, a persistent volume, readiness checks, bounded connection retry behavior, and documented commands for startup and verification.",
      "dependsOn": ["RF-7", "RF-8"],
      "acceptanceIds": ["AC-14"]
    },
    {
      "id": "RQ-2",
      "goalIds": ["G-3"],
      "statement": "Verification shall include pure unit tests and isolated, serial integration tests using real Postgres and real HTTP, covering all six task operations and their principal failure cases.",
      "dependsOn": ["RF-1", "RF-2", "RF-3", "RF-4", "RF-5", "RF-10"],
      "acceptanceIds": ["AC-15"]
    },
    {
      "id": "RQ-3",
      "goalIds": ["G-4"],
      "statement": "The implementation shall use Vertical Slice Architecture with operation-specific request handling, validation, persistence interaction, and response mapping, plus only a minimal shared kernel and no generic Controller, Service, or Repository layers.",
      "dependsOn": [],
      "acceptanceIds": ["AC-16"]
    },
    {
      "id": "RQ-4",
      "goalIds": ["G-4"],
      "statement": "All client-visible validation and dependency-failure responses shall be sanitized and deterministic, and logs shall redact connection secrets and other sensitive configuration values.",
      "dependsOn": ["RF-8", "RF-9"],
      "acceptanceIds": ["AC-12", "AC-17"]
    }
  ],
  "acceptanceCriteria": [
    {
      "id": "AC-1",
      "requirementIds": ["RF-1"],
      "given": "The API and Postgres are available",
      "when": "A client posts a valid non-empty task title",
      "then": "The API returns the created task with an id, the submitted title, and incomplete status, and the record is persisted"
    },
    {
      "id": "AC-2",
      "requirementIds": ["RF-2"],
      "given": "Persisted tasks exist",
      "when": "A client requests the task list",
      "then": "The API returns every available task with id, title, and status in the documented JSON shape"
    },
    {
      "id": "AC-3",
      "requirementIds": ["RF-3"],
      "given": "Persisted tasks exist with more than one status",
      "when": "A client requests a valid status filter",
      "then": "The response contains only tasks with that status"
    },
    {
      "id": "AC-4",
      "requirementIds": ["RF-4"],
      "given": "An incomplete task exists",
      "when": "A client requests completion for that task",
      "then": "The API returns success and subsequent reads show the task as complete"
    },
    {
      "id": "AC-5",
      "requirementIds": ["RF-5"],
      "given": "An existing task and a valid replacement title exist",
      "when": "A client submits the edit request",
      "then": "The API returns success and subsequent reads show the replacement title"
    },
    {
      "id": "AC-6",
      "requirementIds": ["RF-6"],
      "given": "A task has been created and Postgres remains available",
      "when": "Only the API process is stopped and restarted",
      "then": "A subsequent list or get operation returns the same task id, title, and status"
    },
    {
      "id": "AC-7",
      "requirementIds": ["RF-1", "RF-3", "RF-9"],
      "given": "A client submits invalid task input or an invalid status filter",
      "when": "The API validates the request",
      "then": "It returns a stable Problem Details response with an appropriate client error status and no internal diagnostic"
    },
    {
      "id": "AC-8",
      "requirementIds": ["RF-4", "RF-5", "RF-9"],
      "given": "A client references a task that does not exist",
      "when": "The client requests completion or editing",
      "then": "The API returns the documented not-found Problem Details response"
    },
    {
      "id": "AC-9",
      "requirementIds": ["RF-6"],
      "given": "The Postgres service and its persistent volume are available",
      "when": "The API is restarted without recreating the database volume",
      "then": "Previously persisted task data remains available"
    },
    {
      "id": "AC-10",
      "requirementIds": ["RF-7"],
      "given": "A clean local Postgres database is available",
      "when": "The API starts",
      "then": "The required schema is created automatically and a second startup does not fail because the schema already exists"
    },
    {
      "id": "AC-11",
      "requirementIds": ["RF-8"],
      "given": "TODO_DB_CONNECTION is configured or the documented safe local default is used",
      "when": "The application starts",
      "then": "It attempts to connect using that configuration and does not expose credentials in responses or logs"
    },
    {
      "id": "AC-12",
      "requirementIds": ["RF-8", "RF-9", "RQ-4"],
      "given": "Postgres is unavailable or a database operation fails",
      "when": "A client invokes an affected endpoint",
      "then": "The API returns a stable sanitized Problem Details response containing no credentials, stack trace, or provider-internal detail"
    },
    {
      "id": "AC-13",
      "requirementIds": ["RF-10"],
      "given": "An existing task is available",
      "when": "A client requests its removal",
      "then": "The API returns the documented deletion success response and subsequent list or get operations no longer return the task; a missing task returns not-found Problem Details"
    },
    {
      "id": "AC-14",
      "requirementIds": ["RQ-1"],
      "given": "A clean checkout with Docker available",
      "when": "The documented Compose startup and verification sequence is run",
      "then": "Postgres becomes ready through the health check, the API connects with bounded retries, and the sequence completes reproducibly"
    },
    {
      "id": "AC-15",
      "requirementIds": ["RQ-2"],
      "given": "The project is checked out and local dependencies are available",
      "when": "The documented test command is run",
      "then": "Pure unit tests and isolated serial real-HTTP/real-Postgres integration tests execute and cover create, list, filter, complete, edit, remove, validation, not-found, durability, and dependency-failure behavior"
    },
    {
      "id": "AC-16",
      "requirementIds": ["RQ-3"],
      "given": "The implementation is reviewed",
      "when": "The task operation code is inspected",
      "then": "Each operation remains cohesive in its vertical slice and no generic Controller, Service, or Repository layer has been introduced"
    },
    {
      "id": "AC-17",
      "requirementIds": ["RQ-4"],
      "given": "Validation and dependency-failure tests are run",
      "when": "Their responses and captured logs are inspected",
      "then": "The payloads and logs are deterministic and redact credentials, stack traces, and provider internals"
    }
  ],
  "interfaces": [
    {
      "id": "IF-1",
      "kind": "http",
      "name": "Task JSON HTTP API",
      "description": "Unauthenticated local HTTP clients use JSON endpoints for create, list, status filtering, complete, edit, and remove operations.",
      "requirementIds": ["RF-1", "RF-2", "RF-3", "RF-4", "RF-5", "RF-9", "RF-10"]
    },
    {
      "id": "IF-2",
      "kind": "database",
      "name": "Local Postgres",
      "description": "The API reads and writes task records through the Postgres connection configured by TODO_DB_CONNECTION.",
      "requirementIds": ["RF-6", "RF-7", "RF-8"]
    },
    {
      "id": "IF-3",
      "kind": "runtime",
      "name": "Docker Compose local runtime",
      "description": "Docker Compose provides the local Postgres service, readiness check, and persistent volume used by the API and integration tests.",
      "requirementIds": ["RF-6", "RF-7", "RQ-1", "RQ-2"]
    }
  ],
  "dataRules": [
    {
      "id": "DR-1",
      "name": "Task identity and fields",
      "rule": "Every task shall have a generated stable id, a non-empty title after trimming, and a status restricted to the documented incomplete or complete values.",
      "description": "Every task has a generated stable id, a non-empty title, and a status restricted to the documented incomplete or complete values.",
      "requirementIds": ["RF-1", "RF-2", "RF-3", "RF-4", "RF-5", "RF-6", "RF-9"]
    },
    {
      "id": "DR-2",
      "name": "Persistence and deletion",
      "rule": "Task mutations shall be persisted in Postgres; completion and editing shall update the existing record, while removal shall make the record unavailable to subsequent reads.",
      "description": "Task mutations are persisted in Postgres; completion and editing update the existing record, while removal makes the record unavailable to subsequent reads.",
      "requirementIds": ["RF-4", "RF-5", "RF-6", "RF-10"]
    },
    {
      "id": "DR-3",
      "name": "Sanitized failure data",
      "rule": "Client-visible failure payloads and logs shall not include connection credentials, stack traces, or provider-internal diagnostics.",
      "description": "Client-visible failure payloads and logs must not include connection credentials, stack traces, or provider-internal diagnostics.",
      "requirementIds": ["RF-8", "RF-9", "RQ-4"]
    }
  ],
  "delivery": {
    "target": "A greenfield local .NET/C# ASP.NET Core task-management Web API backed by Postgres and Docker Compose, with no frontend, identity, cloud deployment, migration, or regulated-data scope.",
    "verificationStrategy": "Run the documented Docker Compose setup, verify automatic schema initialization and API-only restart durability, then run pure unit tests and isolated serial real-HTTP/real-Postgres integration tests for all six operations and failure contracts.",
    "isBootstrap": true
  }
}
